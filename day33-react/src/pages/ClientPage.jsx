import axios from "axios"
import { useEffect } from "react";

export default function ClientPage() {

  // 서버랑 프론트랑 통신하기
  // useEffect(()=>{

  //     axios
  //       .get("http://localhost:4000")
  //       .then((res) => console.log(res));
  // },[]);



  return (
    <div>
        <h2>서버란?</h2>
        <h4>React - 매가 만든 서버로 이동 - 네이버 응답 - 내가 만든 서버가 - React 결과를 준다.</h4>
        <ol>
          <li>내가 서버를 직접 만든다.</li>
          <li>서버 세팅 (포트번호,express도구!)</li>
        </ol>









        <br /><br /><br /><br /><br />
        <p>인터넷을 통해 요청(Request)을 받으면 그에 맞게 응답(Response)
           을 보내주는 프로그램!
        </p>
        <p>서버 - 뒤에서 일하는 컴퓨터</p>
        <p>서버 - 데이터를 제공하는 컴퓨터(네이버,유튜브)</p>
        <p> 프론트에서 가장 많이 사용하는 서버 - Express </p>

        <h3>Express ?</h3>
        <p> 서버를 아주 쉽게 만드는 도구</p>
        <p>1. 프로젝트 폴더 선택해서 server폴더를 생성</p>
        <p>2. server 이동</p>
        <p>3. npm install express 다운 - 서버에 필요한 도구들 다운받기</p>
        <p>4. 요청이 들어왔을떄 처리하는 파일을 생성 index.js</p>
    </div>
  )
}
/*
┌──────────────────────────────────────────┐
│       1) 컴포넌트가 화면에 처음 렌더됨    │
└──────────────────────────────────────────┘
                    │
                    ▼
┌──────────────────────────────────────────┐
│    2) useEffect(() => { ... }, []) 실행  │
│       (맨 처음 한 번만 실행됨)          │
└──────────────────────────────────────────┘
                    │
                    ▼
┌──────────────────────────────────────────┐
│    3) axios.get("http://localhost:4000") │
│       → 서버에게 요청(Request) 보냄      │
└──────────────────────────────────────────┘
                    │
                    ▼
┌──────────────────────────────────────────┐
│      4) Express 서버가 요청을 받음       │
│         app.get("/") 부분 실행            │
│         res.send("서버 실행 잘됨!")       │
└──────────────────────────────────────────┘
                    │
                    ▼
┌──────────────────────────────────────────┐
│      5) 서버가 응답(Response)을 보냄     │
│         → 브라우저로 JSON/String 전달     │
└──────────────────────────────────────────┘
                    │
                    ▼
┌──────────────────────────────────────────┐
│ 6) axios.then((res) => console.log(res))│
│     → 응답 데이터 콘솔에 출력            │
└──────────────────────────────────────────┘




 */